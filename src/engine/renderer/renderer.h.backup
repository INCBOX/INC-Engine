#pragma once

class Renderer {
public:
    // Initializes OpenGL state (glClearColor, depth test, etc.)
    bool Init();

    // Clears the screen and prepares for rendering
    void RenderFrame(int width, int height);

    // Shuts down the renderer (currently a no-op)
    void Shutdown();
};
